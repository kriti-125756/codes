#include <iostream>

using namespace std;

int main(){
    MaxForOneEle=0;Prev=0,x,CurrVal;
    for(int i=0;i<=N;i++){
    x=i;
    CurrVal = Arr[x];
    Max = CurrVal;Prev=0;
    for(j=x+1;j<=N;j++){
    if(CurrVal+Arr[j] > Max){
        {
        Max=
        }
    }
    }
    }
   /* int NoOfStalls,NoOfStallsToSkip,i,MaxSum;
    int *Arr = new int[NoOfStalls];
    cin>>NoOfStalls>>NoOfStallsToSkip;
    for(i=0;i<NoOfStalls;i++){
    cin>>Arr[i];
    }


    for(i = 0;i<NoOfStalls;i++){
     int x = i;
      MaxSum = 0;
      while(x<=NoOfStalls){
      MaxSum += Arr[x];
      cout<<x;
      x+=NoOfStallsToSkip;
      };
      cout<<"\n";
      //cout<<"\n"<<MaxSum;
    }
*/

	return 0;
}
